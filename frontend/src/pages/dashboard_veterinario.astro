---
const STRAPI_URL = "http://localhost:1337";

// Verificación de autenticación
const jwt = Astro.cookies.get("jwt")?.value;

if (!jwt) {
  console.log("❌ No hay JWT - Redirigiendo a login");
  return Astro.redirect("/login");
}

let userData;
let citas = [];
let disponibilidades = [];

try {
  // Obtener datos del usuario con rol
  const userResponse = await fetch(`${STRAPI_URL}/api/users/me`, {
    headers: { Authorization: `Bearer ${jwt}` },
  });

  if (!userResponse.ok) {
    console.log("❌ JWT inválido - Redirigiendo a login");
    Astro.cookies.delete("jwt", { path: "/" });
    return Astro.redirect("/login");
  }

  userData = await userResponse.json();
  console.log("✅ Usuario autenticado:", userData.username, "| Rol:", userData.role?.name);

  // Verificar que el usuario tiene rol de Veterinario
  const roleName = userData.role?.name?.toLowerCase() || "";
  
  if (!roleName.includes("veterinario")) {
    console.log("⚠️ Usuario no autorizado para este dashboard - Rol:", roleName);
    
    if (roleName.includes("recepcionista")) {
      return Astro.redirect("/dashboard_recepcionista");
    } else if (roleName.includes("cliente")) {
      return Astro.redirect("/cliente_dashboard");
    } else {
      return Astro.redirect("/login");
    }
  }

  console.log("✅ Acceso autorizado a dashboard veterinario");

  // Obtener citas del veterinario
  const citasResponse = await fetch(
    `${STRAPI_URL}/api/citas?filters[veterinario][id][$eq]=${userData.id}&populate=*&sort=fecha:desc`,
    {
      headers: { Authorization: `Bearer ${jwt}` },
    }
  );

  if (citasResponse.ok) {
    const citasData = await citasResponse.json();
    citas = citasData.data || [];
    console.log(`📦 Citas cargadas: ${citas.length}`);
  }

  // Obtener disponibilidades del veterinario
  const disponiblesResponse = await fetch(
    `${STRAPI_URL}/api/disponibles?filters[veterinario][id][$eq]=${userData.id}&populate=*&sort=fecha:desc`,
    {
      headers: { Authorization: `Bearer ${jwt}` },
    }
  );

  if (disponiblesResponse.ok) {
    const disponiblesData = await disponiblesResponse.json();
    disponibilidades = disponiblesData.data || [];
    console.log(`📦 Disponibilidades cargadas: ${disponibilidades.length}`);
  }

} catch (error) {
  console.error("💥 Error verificando usuario:", error);
  return Astro.redirect("/login");
}
import '../styles/dashboard_veterinario.css';
---

<!DOCTYPE html>
<html lang="es">
    <title>Dashboard Veterinario - Veterinaria Pochita</title>
        align-items: center;
        margin-bottom: 0.5rem;
      }

      .appointment-time {
        color: #f39c12;
        font-weight: 600;
        font-size: 1.1rem;
      }

      .appointment-info {
        color: #d0d0d0;
        margin-bottom: 0.3rem;
        font-size: 0.95rem;
      }

      .appointment-info strong {
        color: #ffffff;
      }

      .disponibilidad-item {
        background: rgba(30, 30, 30, 0.5);
        padding: 1rem;
        border-radius: 10px;
        margin-bottom: 1rem;
        border-left: 4px solid #2ecc71;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .disponibilidad-item:last-child {
        margin-bottom: 0;
      }

      .disponibilidad-info {
        flex: 1;
      }

      .disponibilidad-fecha {
        color: #2ecc71;
        font-weight: 600;
        font-size: 1.1rem;
        margin-bottom: 0.3rem;
      }

      .disponibilidad-horario {
        color: #d0d0d0;
        font-size: 0.95rem;
      }

      .disponibilidad-cupos {
        color: #b0b0b0;
        font-size: 0.85rem;
        margin-top: 0.3rem;
      }

      .badge {
        display: inline-block;
        padding: 0.4rem 0.8rem;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
      }

      .badge-pendiente {
        background: rgba(241, 196, 15, 0.2);
        color: #f1c40f;
        border: 1px solid rgba(241, 196, 15, 0.5);
      }

      .badge-confirmada {
        background: rgba(46, 204, 113, 0.2);
        color: #2ecc71;
        border: 1px solid rgba(46, 204, 113, 0.5);
      }

      .badge-cancelada {
        background: rgba(231, 76, 60, 0.2);
        color: #e74c3c;
        border: 1px solid rgba(231, 76, 60, 0.5);
      }

      .badge-completada {
        background: rgba(52, 152, 219, 0.2);
        color: #3498db;
        border: 1px solid rgba(52, 152, 219, 0.5);
      }

      .btn-delete {
        background: rgba(231, 76, 60, 0.9);
        color: white;
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 6px;
        font-size: 0.85rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .btn-delete:hover {
        background: rgba(231, 76, 60, 1);
        transform: translateY(-2px);
      }

      .empty-state {
        text-align: center;
        padding: 2rem;
        color: #b0b0b0;
        font-style: italic;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="header-content">
        <a href="/" class="brand-link">
          <img src="/pochita-logo.png" alt="Pochita" class="logo-img-small" />
          <h1>Dashboard Veterinario</h1>
        </a>
        <div class="header-actions">
          <span class="username">👤 {userData.username}</span>
          <button id="btnCerrarSesion" class="btn-logout">Cerrar sesión</button>
        </div>
      </div>
    </header>

    <div class="container">
      <div class="welcome">
        <h2>Bienvenido, Dr. {userData.username} 👋</h2>
        <p>Gestiona tus citas y disponibilidad</p>
      </div>

      <div class="stats-grid">
        <div class="stat-card">
          <h3>Citas Totales</h3>
          <div class="number">{citas.length}</div>
        </div>
        <div class="stat-card">
          <h3>Citas Pendientes</h3>
          <div class="number">
            {citas.filter((c) => c.estado === "pendiente").length}
          </div>
        </div>
        <div class="stat-card">
          <h3>Citas Confirmadas</h3>
          <div class="number">
            {citas.filter((c) => c.estado === "confirmada").length}
          </div>
        </div>
        <div class="stat-card">
          <h3>Disponibilidades</h3>
          <div class="number">{disponibilidades.length}</div>
        </div>
      </div>

      <div class="grid-2col">
        <!-- Mis Citas -->
        <div class="card">
          <div class="section-header" style="margin-top: 0;">
            <h3>📅 Mis Citas</h3>
          </div>

          {citas.length === 0 ? (
            <div class="empty-state">
              <p>No tienes citas programadas</p>
            </div>
          ) : (
            citas.map((cita) => (
              <div class="appointment-item">
                <div class="appointment-header">
                  <span class="appointment-time">
                    {cita.fecha} - {cita.hora}
                  </span>
                  <span class={`badge badge-${cita.estado || "pendiente"}`}>
                    {cita.estado || "pendiente"}
                  </span>
                </div>
                <div class="appointment-info">
                  <strong>Cliente:</strong> {cita.cliente?.username || "N/A"}
                </div>
                <div class="appointment-info">
                  <strong>Mascota:</strong> {cita.mascota?.nombre || "N/A"}
                </div>
                {cita.motivo && (
                  <div class="appointment-info">
                    <strong>Motivo:</strong> {cita.motivo}
                  </div>
                )}
              </div>
            ))
          )}
        </div>

        <!-- Mis Disponibilidades -->
        <div class="card">
          <div class="section-header" style="margin-top: 0;">
            <h3>🕒 Mis Disponibilidades</h3>
          </div>

          {disponibilidades.length === 0 ? (
            <div class="empty-state">
              <p>No tienes disponibilidades registradas</p>
            </div>
          ) : (
            disponibilidades.map((disp) => (
              <div class="disponibilidad-item">
                <div class="disponibilidad-info">
                  <div class="disponibilidad-fecha">
                    📅 {disp.fecha}
                  </div>
                  <div class="disponibilidad-horario">
                    🕐 {disp.hora_inicio} - {disp.hora_fin}
                  </div>
                  {disp.cupos_disponibles !== undefined && (
                    <div class="disponibilidad-cupos">
                      Cupos: {disp.cupos_disponibles}
                    </div>
                  )}
                </div>
              </div>
            ))
          )}
        </div>
      </div>
    </div>

    <script>
      document.getElementById("btnCerrarSesion")?.addEventListener("click", async () => {
        try {
          const response = await fetch("/api/logout", {
            method: "POST",
          });

          if (response.ok) {
            window.location.href = "/login";
          }
        } catch (error) {
          console.error("Error al cerrar sesión:", error);
          alert("Error al cerrar sesión");
        }
      });
    </script>
  </body>
</html>
