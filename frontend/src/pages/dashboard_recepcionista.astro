---
const STRAPI_URL = "http://localhost:1337";

// Verificación de autenticación
const jwt = Astro.cookies.get("jwt")?.value;

if (!jwt) {
  console.log("❌ No hay JWT - Redirigiendo a login");
  return Astro.redirect("/login");
}

let userData;
let citas = [];
let disponibilidades = [];

try {
  // Obtener datos del usuario con rol
  const userResponse = await fetch(`${STRAPI_URL}/api/users/me`, {
    headers: { Authorization: `Bearer ${jwt}` },
  });

  if (!userResponse.ok) {
    console.log("❌ JWT inválido - Redirigiendo a login");
    Astro.cookies.delete("jwt", { path: "/" });
    return Astro.redirect("/login");
  }

  userData = await userResponse.json();
  console.log("✅ Usuario autenticado:", userData.username, "| Rol:", userData.role?.name);

  // Verificar que el usuario tiene rol de Recepcionista
  const roleName = userData.role?.name?.toLowerCase() || "";
  
  if (!roleName.includes("recepcionista")) {
    console.log("⚠️ Usuario no autorizado para este dashboard - Rol:", roleName);
    
    if (roleName.includes("veterinario")) {
      return Astro.redirect("/dashboard_veterinario");
    } else if (roleName.includes("cliente")) {
      return Astro.redirect("/cliente_dashboard");
    } else {
      return Astro.redirect("/login");
    }
  }

  console.log("✅ Acceso autorizado a dashboard recepcionista");

  // Obtener todas las citas
  const citasResponse = await fetch(
    `${STRAPI_URL}/api/citas?populate=*&sort=fecha:desc`,
    {
      headers: { Authorization: `Bearer ${jwt}` },
    }
  );

  if (citasResponse.ok) {
    const citasData = await citasResponse.json();
    citas = citasData.data || [];
    console.log(`📦 Citas cargadas: ${citas.length}`);
  }

  // Obtener disponibilidades
  const disponiblesResponse = await fetch(
    `${STRAPI_URL}/api/disponibles?populate=*&sort=fecha:desc`,
    {
      headers: { Authorization: `Bearer ${jwt}` },
    }
  );

  if (disponiblesResponse.ok) {
    const disponiblesData = await disponiblesResponse.json();
    disponibilidades = disponiblesData.data || [];
    console.log(`📦 Disponibilidades cargadas: ${disponibilidades.length}`);
  }

} catch (error) {
  console.error("💥 Error verificando usuario:", error);
  return Astro.redirect("/login");
}
import '../styles/dashboard_recepcionista.css';
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard Recepcionista - Veterinaria Pochita</title>
  </head>
  <body>
    <header>
      <div class="header-content">
        <a href="/" class="brand-link">
          <img src="/pochita-logo.png" alt="Pochita" class="logo-img-small" />
          <h1>Dashboard Recepcionista</h1>
        </a>
        <div class="header-actions">
          <span class="username">👤 {userData.username}</span>
          <button id="btnCerrarSesion" class="btn-logout">Cerrar sesión</button>
        </div>
      </div>
    </header>

    <div class="container">
      <div class="welcome">
        <h2>Bienvenido, {userData.username} 👋</h2>
        <p>Gestiona las citas y disponibilidad de los veterinarios</p>
      </div>

      <div class="stats-grid">
        <div class="stat-card">
          <h3>Total Citas</h3>
          <div class="number">{citas.length}</div>
        </div>
        <div class="stat-card">
          <h3>Citas Pendientes</h3>
          <div class="number">
            {citas.filter((c) => c.estado === "pendiente").length}
          </div>
        </div>
        <div class="stat-card">
          <h3>Citas Confirmadas</h3>
          <div class="number">
            {citas.filter((c) => c.estado === "confirmada").length}
          </div>
        </div>
        <div class="stat-card">
          <h3>Disponibilidades</h3>
          <div class="number">{disponibilidades.length}</div>
        </div>
      </div>

      <div class="section-header">
        <h2>Citas Programadas</h2>
        <button class="btn-add" id="btnAgendarCita">+ Agendar Cita</button>
      </div>

      {citas.length === 0 ? (
        <div class="empty-state">
          <p>No hay citas registradas todavía</p>
        </div>
      ) : (
        <div class="table-container">
          <table>
            <thead>
              <tr>
                <th>Fecha</th>
                <th>Hora</th>
                <th>Cliente</th>
                <th>Mascota</th>
                <th>Veterinario</th>
                <th>Motivo</th>
                <th>Estado</th>
                <th>Acciones</th>
              </tr>
            </thead>
            <tbody>
              {citas.map((cita) => (
                <tr>
                  <td>{cita.fecha || "N/A"}</td>
                  <td>{cita.hora || "N/A"}</td>
                  <td>{cita.cliente?.username || "N/A"}</td>
                  <td>{cita.mascota?.nombre || "N/A"}</td>
                  <td>{cita.veterinario?.username || "N/A"}</td>
                  <td>{cita.motivo || "N/A"}</td>
                  <td>
                    <span class={`badge badge-${cita.estado || "pendiente"}`}>
                      {cita.estado || "pendiente"}
                    </span>
                  </td>
                  <td>
                    <div class="actions">
                      {cita.estado === "pendiente" && (
                        <button
                          class="btn-action btn-confirm"
                          data-id={cita.documentId}
                        >
                          Confirmar
                        </button>
                      )}
                      <button
                        class="btn-action btn-cancel"
                        data-id={cita.documentId}
                      >
                        Cancelar
                      </button>
                    </div>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>

    <script>
      document.getElementById("btnCerrarSesion")?.addEventListener("click", async () => {
        try {
          const response = await fetch("/api/logout", {
            method: "POST",
          });

          if (response.ok) {
            window.location.href = "/login";
          }
        } catch (error) {
          console.error("Error al cerrar sesión:", error);
          alert("Error al cerrar sesión");
        }
      });

      // Confirmar cita
      document.querySelectorAll(".btn-confirm").forEach((btn) => {
        btn.addEventListener("click", async (e) => {
          const citaId = e.target.getAttribute("data-id");
          if (!citaId) return;

          try {
            const response = await fetch(`http://localhost:1337/api/citas/${citaId}`, {
              method: "PUT",
              headers: {
                "Content-Type": "application/json",
                Authorization: `Bearer ${document.cookie.split("jwt=")[1]?.split(";")[0]}`,
              },
              body: JSON.stringify({
                data: { estado: "confirmada" },
              }),
            });

            if (response.ok) {
              alert("Cita confirmada exitosamente");
              window.location.reload();
            } else {
              alert("Error al confirmar la cita");
            }
          } catch (error) {
            console.error("Error:", error);
            alert("Error al confirmar la cita");
          }
        });
      });

      // Cancelar cita
      document.querySelectorAll(".btn-cancel").forEach((btn) => {
        btn.addEventListener("click", async (e) => {
          const citaId = e.target.getAttribute("data-id");
          if (!citaId) return;

          if (!confirm("¿Estás seguro de cancelar esta cita?")) return;

          try {
            const response = await fetch(`http://localhost:1337/api/citas/${citaId}`, {
              method: "PUT",
              headers: {
                "Content-Type": "application/json",
                Authorization: `Bearer ${document.cookie.split("jwt=")[1]?.split(";")[0]}`,
              },
              body: JSON.stringify({
                data: { estado: "cancelada" },
              }),
            });

            if (response.ok) {
              alert("Cita cancelada exitosamente");
              window.location.reload();
            } else {
              alert("Error al cancelar la cita");
            }
          } catch (error) {
            console.error("Error:", error);
            alert("Error al cancelar la cita");
          }
        });
      });

      // Agendar cita (placeholder)
      document.getElementById("btnAgendarCita")?.addEventListener("click", () => {
        alert("Funcionalidad de agendar cita próximamente");
      });
    </script>
  </body>
</html>
